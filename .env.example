# Only needed for a PRODUCTION build (npm run build).
# In dev, the Vite proxy in vite.config.js handles /api and this is ignored.
#
# Set this to your deployed backend's base URL (no trailing /api):
# VITE_API_URL=https://ascension-mini-back-production.up.railway.app
VITE_API_URL=
